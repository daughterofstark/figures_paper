# Changes From v5 Referenced

- Replaced only the NS2B-residue-count portion of the Methods metadata placeholder with downstream canonical-table counts: DENV1=50, DENV2=62, DENV3=60, DENV4=31; retained all upstream simulation-metadata placeholders.
- Replaced part of the Data and code availability placeholder with the verified presentation repository remote and current local HEAD, explicitly marked dirty/non-archival.
- Replaced part of the availability text with the verified local per-replicate effect-field table path (`strideouts/outputs/replicate_table.parquet`).
- Inserted the documented presentation build command from `paper_figures/README.md`.
- Inserted verified software/dependency metadata that is directly visible locally: downstream framework project version `0.2.0`, Python `>=3.10`, runtime dependency ranges, and presentation requirements files.
- Left unverified repository URLs, DOI/archive metadata, exact environment versions, final clean commit hash, DENV references, MD setup details, and shared-anchor control as placeholders.
